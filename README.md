# gpull

Actually git pull!

git doesn't have a `git push -f` command, **gpull** is created for this exact purpose.

# install

```sh
npm i -g gpull
```

or

```sh
yarn global add gpull
```

# use

run

```sh
gpull
```
